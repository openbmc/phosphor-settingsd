---
# Settings Config File
org.openbmc.settings.Host:
    powercap:
        name: power_cap
        type: i
        default: 0
        min: 0
        max: 1000
        unit: watts
        validation: range
    bootflags:
        name: boot_flags
        type: s
        default: "default"
        validation: list
        allowed: ["Network", "Disk", "Safe", "CDROM", "Setup", "default"]
    sysstate:
        name: system_state
        type: s
        default: ""
        validation: None
    powerpolicy:
        name: power_policy
        type: s
        default: "RESTORE_LAST_STATE"
        validation: list
        allowed: ["ALWAYS_POWER_ON", "RESTORE_LAST_STATE", "LEAVE_OFF"]
    restrictedmode:
        name: restricted_mode
        type: b
        default: false
        min: 0
        max: 1
        validation: range
    bootpolicy:
        name: boot_policy
        type: s
        default: "ONETIME"
        validation: list
        allowed: ["ONETIME", "PERMANENT"]
    networkconfig:
        name: network_config
        type: s
        default: "ipaddress=,prefix=,gateway=,mac=,addr_type="
        validation: custom
        method: validate_net_config
    TimeMode:
        name: time_mode
        type: s
        default: "NTP"
        validation: list
        allowed: ["NTP", "MANUAL"]
    TimeOwner:
        name: time_owner
        type: s
        default: "BMC"
        validation: list
        allowed: ["BMC", "HOST", "SPLIT", "BOTH"]
    UseDhcpNtp:
        name: use_dhcp_ntp
        type: s
        default: "yes"
        validation: list
        allowed: ["yes", "no"]
    query:
        name: instance_query
        keyregex: "host"
        subtree: "/org/openbmc/control"
        matchregex: "control/(host\d*)$"
org.openbmc.settings.Bmc:
    clock:
        ntp:
            name: ntp
            type: s
            default: "yes"
    dhcp:
        name: dhcp
        default: "yes"
    query:
        name: instance_query
        keyregex: "bmc"
        subtree: "/org/openbmc/control"
        matchregex: "control/(bmc\d*)$"
org.openbmc.settings.Power:
    foozball:
        name: fooz_ball
        type: i
        default: 0
        min: 0
        max: 1000
        unit: watts
        validation: None
    query:
        name: instance_query
        keyregex: "power"
        subtree: "/org/openbmc/control"
        matchregex: "control/(power\d*)$"
org.openbmc.settings.Dimm:
    dimm:
        name: dimm
        default: "yes"
    query:
        name: instance_query
        keyregex: "dimm"
        subtree: "/org/openbmc/inventory"
        matchregex: "/(dimm\d*)$"

# Example of using regex
#    macaddress:
#        name: mac_address
#        type: s
#        default: "aa:bb:cc:dd:ee:ff"
#        validation: regex
#        regex: '([a-fA-F0-9]{2}[:|\-]?){6}'
